import type { ModuleManifest } from '../../types/module.js';

export default {
  name: 'income',
  description: 'Economy features for generating income through various activities and role payouts',
} satisfies ModuleManifest;
